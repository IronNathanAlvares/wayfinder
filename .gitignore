.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
htmlcov/
dist/
build/
*.egg-info/
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
.env

# Never commit credentials. This path is blocked permanently, not by memory.
api_keys.txt
*.key
.secrets/

# The eval verdict cache. Reusable across runs, not evidence, and it can get
# large. The measurements that are evidence live in tests/corpus/measurements/.
.eval-cache.json

# Run logs from the eval comparison. Reproducible from the cache, and the
# measurements that are evidence live in tests/corpus/measurements/.
arms.log
v*run.txt
v*run.json
